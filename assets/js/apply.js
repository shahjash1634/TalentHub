/*function applyJob(jobTitle) {
  let appliedJobs = JSON.parse(localStorage.getItem("appliedJobs")) || [];
  appliedJobs.push({ jobTitle: jobTitle, status: "Pending" });
  localStorage.setItem("appliedJobs", JSON.stringify(appliedJobs));
  alert(`You have applied for ${jobTitle}!`);
}

// Example of job application, replace 'jobTitle' with actual job title in apply.html
document.querySelectorAll(".job-item button").forEach((button) => {
  button.onclick = () => applyJob("Software Developer");
});*/
